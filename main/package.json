{
  "dependencies": {
    "dotenv": "^17.2.2",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "mongoose": "^8.18.1"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:ci": "eslint . --max-warnings=0",
    "test": "jest --runInBand",
    "test:report": "jest --runInBand --coverage --ci --reporters=default --reporters=jest-junit"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.36.0",
    "jest": "^30.2.0",
    "jest-junit": "^16.0.0",
    "mongodb-memory-server": "^10.2.1",
    "supertest": "^7.1.4"
  }
}
