{
  "dependencies": {
    "dotenv": "^17.2.2",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "mongoose": "^8.18.1"
  },
  "scripts": {
    "test": "jest --runInBand",
    "test:report": "jest --runInBand --coverage --ci --reporters=default --reporters=jest-junit"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.1.4"
  }
}
